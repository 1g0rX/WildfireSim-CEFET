#pragma once

// Configurações da simulação
const int MAX_ITERACOES = 100;

// Direções do vento (true para direção ativa)
const bool VENTO_NORTE = false;
const bool VENTO_SUL = false;
const bool VENTO_OESTE = false;
const bool VENTO_LESTE = false;

// Configurações do animal
const int MAX_ITERACOES_PARADO = 3; // Máximo que o animal pode ficar parado